<%= form_with model: @review, url: path do |form| %>
  <% if @review.errors.any? %>
    <ul>
      <% @review.errors.each do |error| %>
        <li><%= error.full_message %></li>
      <% end %>
    </ul>
  <% end %>
  <div>
    <%= form.label :Review %>
    <%= form.text_field :content %>
  </div>
  <div>
    <%= form.label :rating %>
    <%= form.number_field :rating, min: 0, max: 5  %>
  </div>
  <%= form.submit %>
<% end %>